#include <stdio.h>
#include <stdlib.h>
int main ()
{
	int vet[20],i=0,j,v[20],s=0,c;
    scanf ("%d", &vet[i]);
    v[s]=vet[i];
    i=1;
    while (i<20)
    j=0;
    {
    	scanf ("%d", &vet[i]);
    	if (vet[i]==vet[j])
    	{
    		i++;
    		s++;
    		c++;
    		j=0;
		}
		else 
		{
			v[s]=vet[i];
			if (j>20){
				s++;
				i++;
			}
		}
	}
	i=0;
	while (i<20)
	{
		printf ("%d", v[i]);
		i++;
		
	}
}
